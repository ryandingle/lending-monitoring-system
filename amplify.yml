version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 20
        - node --version
        - npm ci
        - npx prisma generate
        - npx prisma migrate deploy
    build:
      commands:
        - env | grep -e APP_URL >> .env.production
        - env | grep -e AUTH_COOKIE_NAME >> .env.production
        - env | grep -e AUTH_SECRET >> .env.production
        - env | grep -e DATABASE_URL >> .env.production
        - env | grep -e SAVINGS_DAILY_INCREMENT >> .env.production
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
